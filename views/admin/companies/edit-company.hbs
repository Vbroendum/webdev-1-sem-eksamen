<form action="/companies/{{company.id}}/edit-company" method="post" class="input-form">
    
    <div class="form-group">
        <label for="name">navn</label>
        <input type="text" name="name" value="{{company.name}}" required> 
    </div>

    <div class="form-group">
        <label for="cvr">Cvr</label>
        <input type="text" name="cvr" value="{{company.cvr}}" required> 
    </div>

    <button type="submit" class="submit-btn">Opdater virksomhed</button>

</form>